# # =============================================================================
# # ApplicationSet: app-resources-by-team-and-category
# # Padrão AppOfApps: 1 Application por TIME + CATEGORIA + ENVIRONMENT
# # Permite agrupamento visual no ArgoCD
# # 
# # Estrutura gerada:
# #   app-default-homolog (app pai)
# #     ├── app-default-homolog-compute-engine
# #     ├── app-default-homolog-pubsub
# #     └── app-default-homolog-storage (se existir)
# #
# # Isso permite: Fácil organização, troubleshooting por categoria, escala
# # =============================================================================
# apiVersion: argoproj.io/v1alpha1
# kind: ApplicationSet
# metadata:
#   name: app-resources-by-category
#   namespace: argocd
# spec:
#   goTemplate: true
#   goTemplateOptions: ["missingkey=error"]
#   generators:
#     # Gera um Application para cada categoria de recurso dentro de cada time/env
#     - git:
#         repoURL: "https://github.com/marcelo-luizz/gitops-app-infra.git"
#         revision: HEAD
#         directories:
#           - path: "environments/*/teams/*/*"  # {env}/teams/{team}/{region}/{category}
#   template:
#     metadata:
#       name: "app-{{index .path.segments 3}}-{{index .path.segments 1}}-{{index .path.segments 4}}"
#       namespace: argocd
#       labels:
#         repo: app-infra
#         team: "{{index .path.segments 3}}"
#         environment: "{{index .path.segments 1}}"
#         resource-type: "{{index .path.segments 4}}"
#         parent: "app-{{index .path.segments 3}}-{{index .path.segments 1}}"
#       finalizers:
#         - resources-finalizer.argocd.argoproj.io
#     spec:
#       project: default
#       source:
#         repoURL: "https://github.com/marcelo-luizz/gitops-app-infra.git"
#         targetRevision: HEAD
#         path: "{{.path.path}}"
#         directory:
#           recurse: true
#           include: "*.yaml"
#       destination:
#         server: https://kubernetes.default.svc
#         namespace: crossplane-system
#       syncPolicy:
#         automated:
#           prune: true
#           selfHeal: true
#         syncOptions:
#           - ServerSideApply=true
#         retry:
#           limit: 3
#           backoff:
#             duration: 5s
#             factor: 2
#             maxDuration: 1m
